{
  "name": "fractal-lab",
  "version": "1.0.0",
  "description": "Realtime fractal art playground powered by Astro and Three.js.",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "tsc --noEmit",
    "astro": "astro",
    "deploy": "bun run build && npx wrangler deploy --config dist/server/wrangler.json",
    "deploy:dry-run": "bun run build && npx wrangler deploy --dry-run --config dist/server/wrangler.json",
    "cf:status": "../scripts/cf-portfolio.sh status fractal-lab",
    "lint": "oxlint",
    "format": "oxfmt",
    "prepare": "husky",
    "lint:fix": "oxlint --fix . && oxfmt"
  },
  "dependencies": {
    "astro": "7.0.7",
    "three": "^0.185.1",
    "wrangler": "4.110.0"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "@astrojs/cloudflare": "14.1.2",
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8",
    "oxfmt": "0.58.0",
    "oxlint": "1.73.0",
    "typescript": "^7.0.2"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "oxlint --fix",
      "oxfmt"
    ]
  }
}
